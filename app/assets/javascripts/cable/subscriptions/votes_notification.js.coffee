# App.votesNotification = App.cable.subscriptions.create "VotesChannel",
#   connected: ->
#     # Called once the subscription has been successfully completed
#
#   received: (data) ->
#     # ok, set all slides to it
#     console.log("OK here's the data", data)
